import Link from 'next/link'; import { Tour } from '@/lib/data';
export default function TourCard({tour}:{tour:Tour}){return <Link href={`/tours/${tour.slug}`} className='block rounded-2xl overflow-hidden shadow bg-white'><div className='h-44 bg-cover bg-center' style={{backgroundImage:`linear-gradient(to top, rgba(0,0,0,.45), rgba(0,0,0,.1)), url(${tour.image})`}}><div className='p-4 h-full flex flex-col justify-end text-white'><h3 className='font-semibold text-xl'>{tour.title}</h3><p>{tour.country} · {tour.format}</p></div></div><div className='p-4'><p>{tour.priceFrom} · {tour.duration}</p></div></Link>}
